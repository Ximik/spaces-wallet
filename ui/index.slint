import { Palette } from "std-widgets.slint";

import { ReceiveAdapter, Receive } from "./pages/receive.slint";
export { ReceiveAdapter }

import { SpacesAdapter, Spaces } from "./pages/spaces.slint";
export { SpacesAdapter }

import { SideNav } from "./components/side_nav.slint";

export component AppWindow inherits Window {
    preferred-width: 700px;
    preferred-height: 500px;
    title: @tr("Spaces Wallet");
    icon: @image-url("../assets/logo.png");

    HorizontalLayout {
        VerticalLayout {
            width: 200px;
            Rectangle {
                background: Palette.control-background;
                Image {
                    source: @image-url("../assets/logo.png");
                    width: 50px;
                    height: 50px;
                    horizontal-alignment: center;
                    image-fit: contain;
                }
            }

            Rectangle {
                side-bar := SideNav {
                    model: [
                        @tr("Home"),
                        @tr("Receive"),
                        @tr("Spaces"),
                    ];
                }
            }
        }

        Rectangle {
            if(side-bar.current-item == 0): Text {
                text: "HOME";
            }
            if(side-bar.current-item == 1): Receive { }
            if(side-bar.current-item == 2): Spaces { }
        }
    }
}
