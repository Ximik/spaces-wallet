export enum ValidatorResult {
    Valid,
    Invalid,
    Reject
}

export global Validators {
    pure callback space_name(string) -> ValidatorResult;
    pure callback coin_address(string) -> ValidatorResult;
}
